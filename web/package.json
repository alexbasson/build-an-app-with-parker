{
  "name": "web",
  "version": "0.0.0",
  "dependencies": {
    "react": "latest",
    "react-dom": "latest",
    "rps": "file:../rps",
    "persistence": "file:../persistence"
  },
  "devDependencies": {
    "babelify": "latest",
    "babel-preset-es2015": "latest",
    "babel-preset-react": "latest"
  },
  "scripts": {
    "compile": "browserify main.js -o bundled/main.js -t [ babelify --presets [ es2015 react ] ]"
  }
}